#include<iostream>
using namespace std;
#define ll long long

/*
#include<iostream>
using namespace std;
int main()
{
	int n;
	cin>>n;
	int A[100000];
	for(int i=0;i<n;i++)
	{
		cin>>A[i];
	}
	
	int lo=0,hi=n-1,mid=0;
	while(mid<=hi)
	{
		if(A[mid]==1)
			mid++;
		else if(A[mid]==2)
		{
			swap(A[mid],A[hi]);
			hi--;
		}
		else
		{
			swap(A[mid],A[lo]);
			lo++;
			mid++;
		}
	}						
	
	for(int i=0;i<n;i++)
	{
		cout<<A[i]<<endl;
	}
	return 0;
}			*/
